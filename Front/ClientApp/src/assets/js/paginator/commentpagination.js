$(document).ready(function() {

        var commentperPage = 1, //number of results per page
            commentwrapper = 'paginationcomment', //wrapper class
            commentlines = 'commentItem', //items class
            commentpaginationId = 'pagination-container', //id of pagination container
            commentpaginationArrowsClass = 'paginacaoCursor', //set the class of pagi
            commentpaginationColorDefault = '#0a0b0c', //default color for the pagination numbers
            commentpaginationColorActive = '#ce3d0e', //color when page is clicked
            commentpaginationCustomClass = 'customPagination'; //custom class for styling the pagination (css)


        /*-------------------------F/ AHMED HIJAZI -------------------------*/
        /*------------------------- HOPE YOU LIKE -------------------------*/


        /*-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------*/
        function paginationShow() {
            if ($("#" + commentpaginationId).children().length > 6) {
                var a = $(".activePagination").attr("data-valor");
                if (a >= 4) {
                    var i = parseInt(a) - 3,
                        o = parseInt(a) + 2;
                    $(".paginacaoValor").hide(), exibir2 = $(".paginacaoValor").slice(i, o).show()
                } else $(".paginacaoValor").hide(), exibir2 = $(".paginacaoValor").slice(0, 5).show()
            }
        }
        paginationShow(),
            // $("#beforePagination").hide(), 
            $("." + commentlines).hide();
        for (var tamanhotabela = $("." + commentwrapper).children().length, porPagina = commentperPage, paginas = Math.ceil(tamanhotabela / porPagina), i = 1; i <= paginas;) $("#" + commentpaginationId).append("<p class='paginacaoValor " + commentpaginationCustomClass + "' data-valor=" + i + ">" + i + "</p>"), i++, $(".paginacaoValor").hide(), exibir2 = $(".paginacaoValor").slice(0, 5).show();
        $(".paginacaoValor:eq(0)").css("color", "" + commentpaginationColorActive).addClass("activePagination");
        var exibir = $("." + commentlines).slice(0, porPagina).show();
        $(".paginacaoValor").on("click", function() {
            $(".paginacaoValor").css("color", "" + commentpaginationColorDefault).removeClass("activePagination"), $(this).css("color", "" + commentpaginationColorActive).addClass("activePagination");
            var a = $(this).attr("data-valor"),
                i = a * porPagina,
                o = i - porPagina;
            $("." + commentlines).hide(), exibir = $("." + commentlines).slice(o, i).show(), "1" === a ? $("#beforePagination").hide() : $("#beforePagination").show(), a === "" + $(".paginacaoValor:last").attr("data-valor") ? $("#afterPagination").hide() : $("#afterPagination").show(), paginationShow()
        }), $(".paginacaoValor").last().after($("#afterPagination")), $("#beforePagination").on("click", function(e) {
            e.stopImmediatePropagation();
            var a = $(".activePagination").attr("data-valor"),
                i = parseInt(a) - 1;
            $("[data-valor=" + i + "]").click(), paginationShow()
        }), $("#afterPagination").on("click", function(e) {
            e.stopImmediatePropagation();
            var a = $(".activePagination").attr("data-valor"),
                i = parseInt(a) + 1;
            $("[data-valor=" + i + "]").click(), paginationShow()
        }), $(".paginacaoValor").css("float", "left"), $("." + commentpaginationArrowsClass).css("float", "left");
    })
    /*-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------*/